<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.databasegroup.dao.ICityDao">
	
	<sql id="columns">cityID, cityName, cityNum</sql>
	
	<select id="getById" parameterType="int"
		resultType="com.databasegroup.model.City">
		SELECT
		<include refid="columns"></include>
		FROM bk_city WHERE cityID=#{id}
	</select>
	
	<select id="getAll"
		resultType="com.databasegroup.model.City">
		SELECT
		<include refid="columns"></include>
		FROM bk_city
	</select>
	
	<insert id="insert" parameterType="com.databasegroup.model.City"
		flushCache="true">
		INSERT INTO bk_city (cityName, cityNum) VALUES
		(#{cityName},#{cityNum})
	</insert>
	
	<update id="update" parameterType="com.databasegroup.model.City">
		UPDATE bk_city SET
		cityName=#{cityName},
		cityNum=#{cityNum}
		WHERE cityID=#{cityID}
	</update>
	
	<delete id="delete" parameterType="com.databasegroup.model.City">
		DELETE
		FROM bk_city
		WHERE cityID=#{cityID}
	</delete> 
</mapper>