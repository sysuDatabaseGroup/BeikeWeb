<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.databasegroup.dao.ISellDao">
	
	<sql id="columns">sellID, userID, methodID, bookID,
		num, datetime, takeBookNum, isMoney</sql>
	
	<select id="getById" parameterType="int"
		resultType="com.databasegroup.model.Sell">
		SELECT
		<include refid="columns"></include>
		FROM bk_sell WHERE sellID=#{id}
	</select>
	
	<select id="getAll"
		resultType="com.databasegroup.model.Sell">
		SELECT
		<include refid="columns"></include>
		FROM bk_sell
	</select>
	
	<insert id="insert" parameterType="com.databasegroup.model.Sell"
		flushCache="true">
		INSERT INTO bk_sell (userID, methodID, bookID, num, 
			datetime, takeBookNum, isMoney) VALUES
		(#{userID},#{methodID},#{bookID},#{num},
		#{datetime},#{takeBookNum},#{isMoney})
	</insert>
	
	<update id="update" parameterType="com.databasegroup.model.Sell">
		UPDATE bk_sell SET
		userID=#{userID},
		methodID=#{methodID},
		bookID=#{bookID},
		num=#{num},
		datetime=#{datetime},
		takeBookNum=#{takeBookNum},
		isMoney=#{isMoney}
		WHERE sellID=#{sellID}
	</update>
	
	<delete id="delete" parameterType="com.databasegroup.model.Sell">
		DELETE
		FROM bk_sell
		WHERE sellID=#{sellID}
	</delete> 
</mapper>